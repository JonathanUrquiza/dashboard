import React, { useState } from 'react'
import Card from './Card'

export default function CardContainer() {
    /* Aca se reciben los datos se mapean y se pasa por props Card
    array.map((item) => <Card item = {item})
    */
      
  return <Card/>
}
